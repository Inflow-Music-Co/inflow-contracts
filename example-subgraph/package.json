{
  "name": "example-subgraph",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ GITHUB_USER/SUBGRAPH_NAME",
    "create-local": "graph create --node http://localhost:8020/ GITHUB_USER/SUBGRAPH_NAME",
    "remove-local": "graph remove --node http://localhost:8020/ GITHUB_USER/SUBGRAPH_NAME",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 GITHUB_USER/SUBGRAPH_NAME"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.21.0",
    "@graphprotocol/graph-ts": "^0.20.1"
  }
}
